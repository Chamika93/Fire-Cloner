{
  "name": "fire-cloner",
  "version": "1.0.0",
  "description": "firestore documents and collections cloner",
  "main": "./lib/index.js",
  "bin": {
    "fire-cloner": "./lib/index.js"
  },
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' 'src/index.ts'",
    "create": "npm run build && npm run test",
    "build": "tsc -p .",
    "test": "sudo npm i -g && fire-cloner",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Chamika93/Fire-Cloner.git"
  },
  "author": "Chamika Adikari",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Chamika93/Fire-Cloner/issues"
  },
  "homepage": "https://github.com/Chamika93/Fire-Cloner#readme",
  "dependencies": {
    "chalk": "^3.0.0",
    "clear": "^0.1.0",
    "commander": "^4.1.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/node": "^13.1.6",
    "nodemon": "^2.0.2",
    "ts-node": "^8.6.1",
    "typescript": "^3.7.4"
  }
}
